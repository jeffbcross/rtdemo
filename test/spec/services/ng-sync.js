'use strict';

describe('Service: ngSync', function () {

  // load the service's module
  beforeEach(module('RTDemoApp'));

  // instantiate service
  var ngSync;
  beforeEach(inject(function (_ngSync_) {
    ngSync = _ngSync_;
  }));

});
